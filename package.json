{
  "name": "bulk-gemini-watermark-remover",
  "version": "1.0.0",
  "description": "Bulk remove Gemini AI watermarks from images — browser UI and CLI",
  "type": "module",
  "scripts": {
    "dev": "node build.js",
    "build": "node build.js --prod",
    "serve": "npx serve dist",
    "bulk": "node dist/cli.js"
  },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.66",
    "jszip": "^3.10.1",
    "medium-zoom": "^1.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "license": "MIT"
}
